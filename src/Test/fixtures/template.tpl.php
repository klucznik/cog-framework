<?php
/**
 * Fixture for TestTemplate. Template::render() extracts the token array into
 * the local scope, so $greeting and $name arrive as plain variables.
 *
 * @var string $greeting
 * @var string $name
 */
?>
<?= $greeting ?>, <?= $name ?>!
